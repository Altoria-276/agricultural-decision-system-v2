### 配置文件说明

`configs/default.yaml` 是默认参数配置文件，包含所有的参数配置

`config/custom.yaml` 是自定义配置文件，custom.yaml 中的项目会覆盖 default.yaml 中的参数配置。可以通过修改 custom.yaml 修改程序的执行参数。

### 配置文件参数说明

- **init_data**：功能为初始参考值，用于余弦相似度过滤和参数选择初始输入。
- **filter_threshold**：功能为余弦相似度过滤阈值；越大保留样本越少，越小保留样本越多，范围 [-1,1]
- **datasets.path**：功能为原始数据文件路径。
- **datasets.sence_columns**：功能为场景特征列名列表。
- **datasets.process_columns**：功能为工艺参数列名列表。
- **datasets.type_columns**：功能为类别标签列名。
- **datasets.result_columns**：功能为结果指标列名。
- **train.test_size**：功能为测试集占比；越大验证越严格，越小训练数据越多。
- **train.random_state**：功能为随机种子；影响可复现性。
- **search_parameters.threshold**：功能为参数搜索的阈值。
- **search_parameters.num_iter**：功能为搜索迭代次数。
- **search_parameters.multiplier**：功能为 目标 Y 值的乘积系数。
- **search_parameters.num_candidates_per_round**：功能为每轮选择的最优参数数量。
- **search_parameters.target_number**：目标 Y 值。
- **select_variables_threshold**：变量筛选的协方差矩阵相关性阈值。
- **select_variables_max_num**：变量筛选个数。

### 临时文件说明

`temp` 目录中保存了程序运行过程中的临时结果和最终结果的图像和 csv 文件，每次运行主函数均会清空 `temp` 目录中的文件。

`temp/csv` 中按照类别保存了每个类别的结果 csv 文件供读取，命名规则为 `f{type}_best_params.csv`

`temp/image` 中按照类别保存了每个类别的 shap 值计算结果和列选择，命名规则为 `f{type}_shap_importance.png`，还包含所有类别的 rmse 值计算 `multi_types_rmse.png`

